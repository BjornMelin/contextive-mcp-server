{
  "name": "contextive-mcp-server",
  "version": "0.1.0",
  "description": "Contextive MCP Server – a lean, multi-provider, workflow-oriented Model Context Protocol server.",
  "bin": {
    "contextive-mcp": "./dist/cli/contextive-mcp.js"
  },
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli/contextive-mcp.ts serve --stdio",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "check": "pnpm run lint && pnpm run test",
    "prepare": "pnpm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "contextive",
    "mcp-server",
    "ai",
    "agents"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.0",
    "ai": ">=6.0.0-beta.143",
    "pino": "^9.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
